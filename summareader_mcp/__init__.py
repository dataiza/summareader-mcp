"""A device that mirrors a SummaReader library.

It pairs with a sync server the way a phone does, holds the decrypted library,
and answers questions about it over MCP, a terminal UI, or a command line.
"""

__version__ = "0.4.5"
