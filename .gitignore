.dart_tool/
pubspec.lock
build/

summareader-mcp.local.json
# The name this file had before the rename, so an old one stays ignored.
allreader-mcp.local.json

# Python
.venv/
__pycache__/
*.egg-info/
.pytest_cache/

# The decrypted library. Rebuilt from the log in about a minute, and the
# single most complete plaintext copy of it that exists — not something to
# hand to a git remote.
.cache/
